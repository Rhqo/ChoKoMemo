extends topBar

block content
  .content
    .leftsideBar 
      each memo in memoList
        if memo.memoId == memoDetail.memoId
          li.selected
            a(href="/memo?id="+memo.memoId)= memo.title
        else 
          li
            a(href="/memo?id="+memo.memoId)= memo.title
    .rightsideBar
      h1= memoDetail.title 
      p= memoDetail.content
